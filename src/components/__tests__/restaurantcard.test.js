import RestaurantCard from "../RestaurantCard"

const { render } = require("@testing-library/react")

it("should render res card",()=>{
    // render(<RestaurantCard resData={}/>)
    // will cretea mocks data file

})